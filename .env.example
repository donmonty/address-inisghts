# Unrestricted public token, server-only. URL restrictions would 403 from a
# server, which sends no Referer — see docs/research/mapbox-limits-and-tokens.md.
MAPBOX_SERVER_TOKEN=

# Public token, URL-restricted to localhost and both production hosts. Ships to
# the browser for GL JS tiles.
NEXT_PUBLIC_MAPBOX_TOKEN=
